streamlit==1.45.1
streamlit-authenticator>=0.4
pandas>=2.0
plotly>=5.18
requests>=2.31
supabase>=2.0
python-dotenv>=1.0
bcrypt>=4.0
pillow>=10.0
toml>=0.10
openpyxl>=3.0

# Backend FastAPI (servicio paralelo Railway, comparte el repo)
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.7
pydantic-settings>=2.4
httpx>=0.27
python-multipart>=0.0.9
PyJWT>=2.8
email-validator>=2.1
playwright>=1.40

# Claude AI (auditoría conversaciones + informe consultor + OCR remisiones)
anthropic>=0.40

# PDF → imagen (para OCR de remisiones de textilera con Claude vision).
# pypdfium2 no requiere binarios del sistema (funciona en Railway).
pypdfium2>=4.30

# PDF de etiquetas Zebra 10×10 con Code128 (produccion.ingreso).
reportlab>=4.0

# Tests
pytest>=8.0
